/FIXME
%h1 {{mailApp.name}}

%pre
  %code
    = "config.action_mailer.smtp_settings = {"
    = " :address => '{{mailApp.settings.address}}',"
    = " :port => '{{mailApp.settings.port}}',"
    = " :user => '{{mailApp.name}}',"
    = " :password => '{{mailApp.settings.password}}'"
    = "}"
.container
  .row
    .span8
      = render "web/shared/messages_list"
    .span4{ "ng-bind-html" => "resourceMessageBody" }
